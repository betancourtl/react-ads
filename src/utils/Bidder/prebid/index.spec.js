import prebid from './';

describe('prebid test', () => {

  test('prebid.init', () => {
  });

  test('prebid.fetchBids', () => {
  });

  test('prebid.onBidWon', () => {
    expect(prebid.onBidWon()).toBe(undefined);
  });

  test('prebid.onBidWon', () => {
    expect(prebid.onBidWon()).toBe(undefined);
  });
});